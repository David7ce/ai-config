# Generated by `node cli/index.js` — .ai/ is the source of truth, these are build output.
# Run the CLI after cloning to materialize them locally; never hand-edit or commit them.
/CLAUDE.md
/AGENTS.md
/GEMINI.md
/.windsurfrules
/.mcp.json
/.cursor/
/.claude/
/.opencode/
/.github/
/.vscode/

node_modules/

# Safety net: never let a stray credentials file into the dotfiles source, even by accident.
.ai/**/.credentials.json
.ai/**/*.token
# Safe test directories
tmp/
