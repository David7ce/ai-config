{
  "name": "ai-config",
  "version": "0.1.0",
  "description": "Cross-platform, cross-agent dotfiles: generate per-tool AI agent config (Claude, Codex, opencode, Gemini, Cursor, Windsurf, Copilot, MCP) and apply this machine's Claude Code dotfiles, all from a single .ai/ source of truth.",
  "private": true,
  "bin": {
    "ai-config": "./cli/index.js"
  },
  "scripts": {
    "test": "node cli/test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
